# Agents

# Agents

This folder defines the agent execution model for `app_category_registry`.

It should describe:

- allowed agents
- blocked agents
- agent assignments
- agent logs

The plugin uses agents only as governed workers, not as hidden decision makers.
