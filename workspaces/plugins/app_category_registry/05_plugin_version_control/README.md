# Version Control

# Version Control Governance

This folder defines how `app_category_registry` should interact with Git, branches, pull requests, issues, releases, and ownership history.

It should cover:

- branch strategy
- commit rules
- issue tracking
- pull request review
- release tagging
- ownership history

The plugin uses this layer to plan, not to publish automatically.
