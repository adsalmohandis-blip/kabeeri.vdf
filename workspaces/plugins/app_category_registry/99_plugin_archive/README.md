# Archive

# Archive

This folder stores archived mappings, old records, and historical evidence bundles for `app_category_registry`.

It should preserve:

- implementation history
- compatibility mappings
- archived evidence bundles
- deprecated planning notes

The archive is a record, not an active workspace surface.
