# App Folder Structure

Plugin Development Track workspace for `app_folder_structure`.

This workspace is the planning and governance source of truth for the plugin package that lives in:

- `04_plugin_package/`

The workspace tracks:

- input intake
- plugin identity
- roadmap planning
- specifications
- version control governance
- evolutions
- task punches
- agents
- source tracking
- tests and quality
- evidence and audit
- reviews and approvals
- package release
- documentation
- archive mappings

The workspace uses underscore slugs and preserves the canonical full folder set.

