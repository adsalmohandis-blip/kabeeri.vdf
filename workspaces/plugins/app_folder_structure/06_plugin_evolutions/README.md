# Evolutions

The detailed Evo queue for `app_folder_structure` is stored here, with the index as the source of truth.

Each Evo folder documents a phase slice from repo analysis through final acceptance. The planning set is preserved here even after implementation so the workspace keeps its development history.
