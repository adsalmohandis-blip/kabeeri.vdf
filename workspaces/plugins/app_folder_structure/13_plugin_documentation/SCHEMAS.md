# Schemas

The plugin uses schemas to keep the contract explicit and machine-readable.

Key schema areas:

- app workspace manifest
- category folder-structure profile
- app workspace validation report

These schemas support safe create, validate, repair, and manifest generation without guessing at the folder structure.

Documents the schema files used by app workspace manifests and category profiles.
