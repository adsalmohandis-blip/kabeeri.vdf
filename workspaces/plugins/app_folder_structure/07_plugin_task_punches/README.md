# Task Punches

The detailed task-punch queue for `app_folder_structure` is stored here, with the index as the source of truth.

Task punches keep implementation work narrow and auditable. They are organized so each punch can be completed and validated without changing unrelated workspace paths.
