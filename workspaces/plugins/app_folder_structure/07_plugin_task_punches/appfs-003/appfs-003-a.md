# appfs-003-a

- Goal: Create the fixed app pipeline skeleton.
- Meaning: Create the canonical app workspace root and the top-level pipeline folders only.
- Allowed files:
  - workspaces/apps/<app_slug>/
  - app folder templates
  - docs
- Forbidden files:
  - Source-layout forcing
- Acceptance: A new app workspace has the canonical top-level pipeline.
- Tests / checks:
  - Create/structure tests.
