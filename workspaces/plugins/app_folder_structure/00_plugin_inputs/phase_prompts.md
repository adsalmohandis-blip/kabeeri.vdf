# Phase Prompts

This workspace keeps the original `app_folder_structure` phase prompts here as input history.

## Canonical intent
- create and validate app workspaces under `workspaces/apps/<app-slug>/`
- keep a fixed app pipeline
- integrate category-approved roadmap profiles
- keep `08_source` stack-adaptive
- generate manifests, evidence, validation, and repair records safely
- preserve existing compact folders if they already exist

## Correction notes
- the full canonical folder set is the active target
- compact folders are backward-compatible migration history
- no deep plugin-root nesting should be introduced