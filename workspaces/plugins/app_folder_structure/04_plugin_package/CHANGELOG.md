# Changelog

## 1.0.0

- Rebuilt `app_folder_structure` as the app-workspace contract engine.
- Added fixed app pipeline creation, validation, repair, manifest, and category profile commands.
- Switched workspace slugs to underscore-canonical naming.
