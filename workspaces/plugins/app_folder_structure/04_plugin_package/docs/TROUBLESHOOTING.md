# Troubleshooting

- If create or validate fails, confirm the app slug is safe and the category profile exists.
- If the source folder looks too opinionated, remember `08_source/` stays stack-adaptive.
- If files were not written, check whether the workspace already contained non-empty files that were intentionally preserved.
