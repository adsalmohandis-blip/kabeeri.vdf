# Overview

`app_folder_structure` is the app-workspace contract engine for KVDF/KVDOS.

It governs:

- the fixed app pipeline under `workspaces/apps/<app-slug>/`
- category-gated roadmap internals
- stack-adaptive source tracking
- manifests, validation, repair, and evidence

It does not invent source layouts. It only creates the governed workspace contract.
