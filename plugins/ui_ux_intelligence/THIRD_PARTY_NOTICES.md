# Third-Party Notices

This plugin is built from scratch for KVDF Core.

- `ui_ux_intelligence` is a standalone KVDF plugin.
- The selected CSV data files were provided locally by the Owner through the plugin `_temp_meta/` staging folder.
- No external GitHub dependency is required for the relocated catalog or search engine.
- Only the selected CSV data files were relocated into `data/` and `data/stacks/`.
- Reference scripts and templates from `_temp_meta/` were used only as implementation guidance and were not copied into runtime code.
- Font binaries, screenshots, previews, packaging, and unrelated wrappers were excluded.
