# Overview

`plugin_dev` is the plugin-development orchestrator that sits after workspace creation.

It validates that the workspace is contract-compliant, then helps generate intake, analysis, specs, tasks, source, tests, evidence, readiness, packages, and promotion requests.
