# Git Library Inputs

Git library inputs are analyzed first and reused only with explicit review.

`plugin_dev` must not:
- execute unknown code
- install dependencies automatically
- copy code blindly
- ignore license or attribution requirements
