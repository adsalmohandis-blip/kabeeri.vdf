# CRM Builder

`crm` is the app-track plugin for customer relationship management systems.

```bash
kvdf plugins install crm
kvdf plugins uninstall crm
kvdf crm status
kvdf crm init --mode sales
```

The live runtime state lives in `.kabeeri/crm.json`.
