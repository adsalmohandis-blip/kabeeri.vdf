# Plugin Folder Structure

Track-aware plugin folder governance for KVDF / KVDOS.

- No numbered folders.
- No folder names start with `plugin_`.
- Owner Track creates directly under `./plugins/<plugin-slug>/`
- Plugin Development Track creates under `./workspaces/plugins/<plugin-slug>/`
- Viber/App Track is blocked from direct plugin creation
