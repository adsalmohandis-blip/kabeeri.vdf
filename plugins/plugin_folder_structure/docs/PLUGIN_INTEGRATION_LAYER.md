# Plugin Integration Layer

Integration is first-class for both owner and plugin_dev flows.

Owner plugins keep integration metadata in `plugin.json` and `docs/INTEGRATIONS.md`.

Plugin Development Track workspaces keep integration plans and contracts in the workspace folders.
