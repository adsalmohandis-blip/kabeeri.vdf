# Owner Track Direct Plugin Flow

1. Choose `--track=owner`.
2. Create the plugin directly in `./plugins/<plugin-slug>/`.
3. Use the same shell vocabulary as the workspace model without adding numbered folders.
4. Validate the direct plugin shell.
5. Continue owner development without workspace promotion.
