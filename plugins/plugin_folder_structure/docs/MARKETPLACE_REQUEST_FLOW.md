# Marketplace Request Flow

Marketplace upload requests are request-only.

They do not publish anything.
They only record readiness, evidence, and the requested destination.

The sanitized marketplace bundle is produced from `./workspaces/plugins/<plugin-slug>/04_plugin_package/` plus public-facing release artifacts only.
