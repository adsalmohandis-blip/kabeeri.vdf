# Direct Install Request Flow

Plugin Development Track workspaces may request direct install, but promotion is still controlled by Owner Track.

The request must point at `./workspaces/plugins/<plugin-slug>/04_plugin_package/`.
