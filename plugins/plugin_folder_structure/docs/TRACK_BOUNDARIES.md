# Track Boundaries

## Owner Track

Owner Track may create a plugin directly in `./plugins/<plugin-slug>/` using the shared shell folders and the same non-numbered vocabulary.

## Plugin Development Track

Plugin Development Track may create a workspace in `./workspaces/plugins/<plugin-slug>/` and later request review or marketplace upload.

## Viber/App Track

Viber/App Track must not create plugin folders directly.
