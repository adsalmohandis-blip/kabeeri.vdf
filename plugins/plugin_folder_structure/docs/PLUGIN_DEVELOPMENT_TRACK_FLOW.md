# Plugin Development Track Flow

1. Choose `--track=plugin_dev`.
2. Create the plugin workspace in `./workspaces/plugins/<plugin-slug>/`.
3. Collect inputs, identity, plans, specs, source, evidence, and review artifacts.
4. Validate readiness.
5. Request owner review or marketplace upload only when ready.
